# git-versioner

CLI di versioning per i progetti git:

- monorepo
- multi-repo
- app + layers

## Uso
- [GUIDA](./docs/GUIDA.md)

```bash
node bin/git-versioner.mjs --config ./version.config.mjs --commit --push